server:
  port: 8080

spring:
  application:
    name: testLatticeApp

ribbon:
  ServerListRefreshInterval: 1000

endpoints:
  health:
    sensitive: false
  restart:
    enabled: true
  shutdown:
    enabled: true

kafka:
  bootstrap-servers: localhost:9092
  topic:
    helloworld: helloworld.t